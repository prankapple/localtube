<nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
  <div class="container">
    <a class="navbar-brand" href="/"><img src="/logo.png" height="40"> LocalTube</a>
    <form class="d-flex" action="/" method="GET">
      <input class="form-control me-2" type="search" placeholder="Search" name="q">
      <button class="btn btn-outline-success" type="submit">Search</button>
    </form>
    <div>
      <% if(user){ %>
        <span>Hello, <%= user.username %></span>
        <a href="/upload" class="btn btn-primary btn-sm">Upload</a>
        <a href="/logout" class="btn btn-outline-secondary btn-sm">Logout</a>
      <% } else { %>
        <a href="/login" class="btn btn-outline-primary btn-sm">Login</a>
        <a href="/register" class="btn btn-success btn-sm">Register</a>
      <% } %>
    </div>
  </div>
</nav>
